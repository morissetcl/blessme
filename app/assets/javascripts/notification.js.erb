function update() {
  var currentLocation = window.location;
  $.ajax({
    url: currentLocation,
    success: function(data) {
      $('.navbar-blessme').html(data);
    }
  });
};
setInterval(update(), 10000);
